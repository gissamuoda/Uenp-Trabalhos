Nome: Gabriel Issamu Oda Ferreira Atividade Avaliativa 2 1 -  O efeito de declarar um campo como static seria o de não poder mudar o valor atribuido a variável com esse campo. Como por exemplo, na classe Alpha, em que o valor int atribuido a variável alpha, que seria 0 (zero), não pode ser alterado em lugar nenhum dentro e fora da classe 2- public static int mostrar(String... strings){	int i;	for(i = 0; i < strings.length; i++){		System.out.println(strings[i]);	}	return i;}3 - => Caixa.javapublic class Caixa {    private int flag;        Caixa(){        this.flag = 0;    }    Caixa(int num){        this.setFlag(num);     }    public void setFlag(int num){        this.flag = num;    }    public int getFlag(){        return this.flag;    }    public static void zerar(Caixa box){        box.setFlag(0);    }}=> Main.javapublic class Main {    public static void main(String[] args) {        Caixa box = new Caixa(10);        System.out.println(box.getFlag());        Caixa.zerar(box);        System.out.println(box.getFlag());    }}4 - Irá ocorrer erro de compilação na lina 7 do método main(), já que nessa linha está tentando acessar uma variável private, em que é uma variável visivel apenas dentro da classe